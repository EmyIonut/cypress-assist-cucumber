{
  "name": "cypress-assist-cucumber",
  "version": "1.0.0",
  "description": "ASSIST cypress with cucumber framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ASSIST",
  "license": "ISC",
  "dependencies": {
    "cypress": "^7.3.0"
  },
  "devDependencies": {
    "cypress-cucumber-preprocessor": "^4.1.0"
  },
  "cypress-cucumber-preprocessor": {
    "step_definitions": "cypress/support/step_definitions/**/"
  }
}
